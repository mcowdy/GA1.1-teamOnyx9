# GA1.1-teamOnyx9
Group Assignment 1.1
## function `multiply`
Multiplier added. This function allows someone to input two number, which are then multiplied and the numbers are the shown. 
## function `subtract`
takes two numbers and the output is the difference between them
it breaks if an array or string is entered 
## function `add`
subgroup a made a function that can add numbers. It works with any digits. example add(a, b) so fill in numbers at a and b.
## function `sum_list`
This function takes in a list of values, and returns the sum of all of them.
For example:
```
sum_list([1,2,3,4])
-> 10
```
## function `zero`
Added the distance fom zero function. This function has 1 input x, which is then put in the subtraction function to determine how far it is from 0. 
subgroup added a feature to the multiply function that squares the outcome of the multiply function. It works on any digit and it doesn't work on lists.

